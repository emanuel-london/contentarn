import * as Types from "../action/types";

let initState = {

};

export const dashboardReducer = (state = initState, action) => {
  switch (action.type) {
    default: {
      return {
        ...state
      };
    }
  }
};
